import React from 'react';

export default function LoginClubLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
        <div>
                    {children}
                </div>
    );
}