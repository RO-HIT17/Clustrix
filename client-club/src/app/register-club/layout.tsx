import React from 'react';

export default function RegisterClubLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
        <div>
                    {children}
                </div>
    );
}